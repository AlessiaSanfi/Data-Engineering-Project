name: Olist Daily Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'

jobs:
  run-etl:
    runs-on: ubuntu-latest
    env:
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install pandas pyarrow fastparquet duckdb google-genai python-dotenv pandera

      - name: Run Incremental Ingestion (Bronze to Silver)
        run: python etl/tasks/bronze_incremental.py

      - name: Run Star Schema Transformation (Silver to Gold)
        run: python etl/tasks/silver_to_gold.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add -f data/lake/gold/*.parquet 
          git commit -m "auto: update gold layer [skip ci]" || echo "No changes to commit"
          git push
